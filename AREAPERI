#include <stdio.h>

int main() {
    int L, B;
    int a, p;
    scanf("%d\n%d", &L, &B);
    a = L * B;
    p = (L+B)*2;
    if (a>p) {
        printf("Area\n%d", a);
    } else if (p>a) {
        printf("Perimeter\n%d", p);
    } else {
        printf("Eq\n%d", a);
    }
    
	return 0;
}
